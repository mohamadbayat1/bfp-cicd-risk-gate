# Data (large raw CSV — never commit)
*.csv

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Generated artifacts & models (reproducible from the pipeline)
models/
artifacts/
*.joblib
*.pkl
*.png

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Nested repos / generated working copies
demo-app/
campaign/work/

# Local documents & working notes (generic patterns only;
# specific private filenames are kept in .git/info/exclude, which is never pushed)
*.docx
*.rar
~$*
