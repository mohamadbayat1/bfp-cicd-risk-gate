# Data (large raw CSV — never commit)
*.csv
final-2017.csv

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Generated artifacts & models (reproducible from the pipeline)
models/
artifacts/
*.joblib
*.pkl
*.png

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
.github_token

# Nested repos / generated working copies (each campaign repo is its own git repo)
demo-app/
campaign/work/

# Thesis manuscripts & supervisor correspondence stay off GitHub (shared privately)
*.docx
~$*
master.md
chapter_fixes/

# Legacy docx build toolchain (local only)
chapter4_docx/
